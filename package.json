{
  "name": "mockup-ai-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SiberCraft session-based AI mockup workspace powered by DeepSeek",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
